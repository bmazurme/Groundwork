# Groundwork Cypress Fixture

This document exists only for end-to-end tests. It mentions the unique
marker phrase `zzqx-cypress-marker` so the search test can look it up
after indexing finishes.

## Section

Some filler prose to give the chunker more than one paragraph to work
with, so the structure-aware chunking has real boundaries to respect.
